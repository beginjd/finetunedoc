# Fine-tuning dependencies for Mistral 7B with QLoRA on AMD MI300X (ROCm)
# Note: PyTorch comes pre-installed in ROCm container images
# bitsandbytes is installed separately from ROCm-compatible wheel in start_amd.sh

# Core ML libraries (PyTorch excluded - comes with ROCm container)
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.6.0
# bitsandbytes installed separately from ROCm wheel in startup script

# Training
trl>=0.7.0
datasets>=2.14.0

# Tokenization (required for Mistral)
# Note: sentencepiece and protobuf installed separately in startup script
# sentencepiece>=0.1.99
# protobuf>=3.20.0

# Utilities
tensorboard>=2.14.0
safetensors>=0.4.0

# Optional but recommended
scipy>=1.11.0
scikit-learn>=1.3.0
